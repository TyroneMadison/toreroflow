/**
 * Toreroflow worker — BullMQ media/publish/analytics workers arrive in M2/M3.
 * M0 scaffolds the app so the workspace shape matches spec Section 6.
 */
console.log("[toreroflow-worker] scaffold only — workers activate in M2 (media) and M3 (publish).");
